name: logic_lab
description: Fauzi — Senior Mobile Application Engineer Portfolio
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  go_router: ^17.0.1
  google_fonts: ^6.2.1
  http: ^1.6.0
  responsive_framework: ^1.4.0
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - output/pdf/Fauzi-CV.pdf
    - assets/video/hero-cinematic.mp4
    - assets/video/hero-cinematic-poster.jpg
